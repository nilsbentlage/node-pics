# Token for clearing cache (use a strong, random string)
CLEAR_CACHE_TOKEN=your-secret-token-here

# Token for embedding the clear-cache-button (use a strong, random string)
EMBED_TOKEN=false

# External port for Docker (defaults to 3000 if not set)
EXTERNAL_PORT=3000

# Optional: Node environment
NODE_ENV=production